<!-- AUTO-GENERATED by scripts/lua2md.ts — edit .vitepress/nav.ts and re-run `pnpm lua2md` -->
# Modules

Configure tools such as Git, Zsh, and SSH from your Lua configuration.

Pull a module in with `require("rootbeer.<name>")`:

```lua
local zsh = require("rootbeer.zsh")
local git = require("rootbeer.git")
```

Use [Profiles](/guide/profiles) for settings that differ between machines.
For tools without a module, write their configuration files with the
[core API](/reference/core).

## Shell

| Page | Description |
| ---- | ----------- |
| [`zsh`](/modules/zsh) | `~/.zshenv`, `.zprofile`, `.zshrc` from one table. |

## Developer Tools

| Page | Description |
| ---- | ----------- |
| [`git`](/modules/git) | `~/.gitconfig` and global gitignore. |
| [`ssh`](/modules/ssh) | `~/.ssh/config` host blocks and includes. |

## AI Coding

| Page | Description |
| ---- | ----------- |
| [`amp`](/modules/amp) | Amp settings, MCP servers, AGENTS.md. |
| [`claude_code`](/modules/claude-code) | Claude Code settings, permissions, LSP. |

## Package Managers

| Page | Description |
| ---- | ----------- |
| [`brew`](/modules/brew) | Brewfile + `brew bundle` for formulae, casks, MAS. |

## System

| Page | Description |
| ---- | ----------- |
| [`mac`](/modules/mac) | macOS Dock, Finder, hot corners, hostname, Touch ID. |
