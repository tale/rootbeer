pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --all
      stage_fixed: true
    clippy:
      glob: "*.rs"
      run: cargo clippy --workspace --fix --allow-dirty --allow-staged -- -D warnings
      stage_fixed: true
    oxfmt:
      glob: "*.{js,ts}"
      run: pnpm oxfmt --write {staged_files}
      stage_fixed: true
