[package]
name = "rootbeer-core"
version = "0.1.0"
edition = "2021"

[features]
default = ["embedded-stdlib"]
embedded-stdlib = []

[dependencies]
libc = "0.2.182"
mlua = { version = "0.11.5", features = ["luau-jit"] }
serde_json = "1"
toml = "1"

[dev-dependencies]
tempfile = "3"
